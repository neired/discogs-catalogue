import React from 'react';
import './Header.scss';

const Header = props => {
//   const { } = props;
  return (
    <header className="App-header">
      <h1>Discogs Catalogue</h1>
    </header>
  )
}

export default Header;